sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t rwedwards/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true rwedwards/react-test npm test

after_success:
  - docker build -t rwedwards/multi-client ./client
  - docker build -t rwedwards/multi-nginx ./nginx
  - docker build -t rwedwards/multi-server ./server
  - docker build -t rwedwards/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push rwedwards/multi-client
  - docker push rwedwards/multi-nginx
  - docker push rwedwards/multi-server
  - docker push rwedwards/multi-worker
